const result = amount.add(fee);
const diff = balance.sub(cost);
const product = price.mul(quantity);
const share = total.div(count);
const remainder = value.mod(divisor);
const chained = amount.add(fee).mul(rate);
