const result = (amount + fee);
const diff = (balance - cost);
const product = (price * quantity);
const share = (total / count);
const remainder = (value % divisor);
const chained = ((amount + fee) * rate);
